apiVersion: v1
kind: ConfigMap
metadata:
  name: family-task-config
  namespace: {{ .Values.namespace }}
data:
  DB_HOST: {{ .Values.config.dbHost | quote }}
  DB_PORT: {{ .Values.config.dbPort | quote }}
  DB_NAME: {{ .Values.config.dbName | quote }}
  PORT: {{ .Values.config.backendPort | quote }}
